[project]
name = "demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jeroen Vervaeke", email = "jeroen.vervaeke@mongodb.com" }
]
requires-python = ">=3.10"
dependencies = [
    "atlas-local-lib-py>=1.0.0",
    "langchain>=1.4.2",
    "langchain-mongodb>=0.11.0",
    "langchain-openai>=1.6.2",
    "marimo>=0.24.2",
    "pymongo>=4.18.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
demo = "demo:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
